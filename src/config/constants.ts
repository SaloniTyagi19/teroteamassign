const ROLES = {
    ADMIN: 'Admin',
    USER: 'User'
};

const STATUS = {
    ACTIVE: 'Active',
    DELETE: 'Delete'
};

const TOKEN_TYPE = {
    ACCESS_TOKEN: '1',
    REFRESH_TOKEN: '2'
};

export default {
    ROLES,
    TOKEN_TYPE,
    STATUS
}